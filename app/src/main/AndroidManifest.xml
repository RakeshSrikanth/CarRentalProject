<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rockooapps.carrentals">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rockoorentals"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Login_User">

        </activity>
        <activity android:name=".Registration_user" />
        <activity android:name=".Login_Admin" />
        <activity android:name=".CarBookPage" />
        <activity android:name=".BookingDatesPage" />
        <activity android:name=".RegisteredUsers_Admin" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AdminPass_Change" />
        <activity android:name=".Admin_AddCars" />
        <activity android:name=".User_CarDetails"></activity>
    </application>

</manifest>